export interface PanState {
  translateX: number;
  translateY: number;
  isPanning: boolean;
}

export interface TransformConfig {
  scale: number;
  translateX: number;
  translateY: number;
}

export interface ViewportOffset {
  offsetX: number;
  offsetY: number;
}

